// This test requires real uikit components and Three.js dependencies
// Moving to e2e tests since unit testing requires complex mocking
// that doesn't align with the repository's testing patterns

describe.skip("deserializer - moved to e2e", () => {
  it("should be tested in e2e environment", () => {
    // This test suite has been moved to e2e testing
    // since it requires real Three.js and uikit components
  });
});